import React from 'react'
import ReactDOM from 'react-dom/client'
import CommandOverlay from './views/CommandOverlay'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CommandOverlay />
  </React.StrictMode>
)
